<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:paddingLeft="20dp"
    android:paddingRight="20dp"
    android:layout_width="match_parent"
    >
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="?android:attr/actionBarSize"
        android:orientation="horizontal"
        android:layout_marginTop="@dimen/margin_very_big"
        >

        <Button
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:id="@+id/post_btn"
            android:textColor="@color/greenyBlue"
            android:text="Posts"
            android:background="@drawable/first_tab_select_states"/>
        <Button
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:id="@+id/map_btn"
            android:textColor="@color/greenyBlue"
            android:text="Maps"
            android:background="@drawable/middle_select_states"/>
        <Button
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:textColor="@color/greenyBlue"
            android:id="@+id/network_btn"
            android:text="Network"
            android:background="@drawable/second_tab_select_states"/>


    </LinearLayout>
    <android.support.v4.view.ViewPager
        android:layout_width="match_parent"
        android:id="@+id/my_req_viewpager"
        android:layout_height="match_parent"></android.support.v4.view.ViewPager>



</LinearLayout>
